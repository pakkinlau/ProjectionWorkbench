# F.RUV.W3Q2Q.B1 — Corrected Target Schema / Currentness / Replay Qualification

This branch re-runs only Q7 against `FinalIntegratedW3RTarget.v2`.

It consumes the corrected content-addressed target carrier by immutable GitHub Actions artifact pointer, verifies the target identity and 171-file manifest, executes the full integrated suite, reruns the 31-case R1–R5 repair witness, runs four isolated source/wheel fresh-agent R6 probes, and verifies target nonmutation.

The branch contains no prior-wave archive, target archive, repository clone, virtual environment, wheel, dependency tree, cache, or build output. Those objects are retrieved or reproduced during CI and returned only as compact receipts.

A passing branch is one Q7 qualification candidate. It cannot open Wave 4 without `F.RUV.W3Q2Q.B2` and the conjunctive N3 merge.
