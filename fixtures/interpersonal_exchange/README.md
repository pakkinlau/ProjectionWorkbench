# Interpersonal exchange fixture

Run:

```bash
python scripts/run_interpersonal_exchange_witness.py all \
  /tmp/f2-w3-b3-work /tmp/f2-w3-b3-out
```

The fixture creates two isolated local instances and a transport directory. Only
transport artifacts cross the instance boundary.
