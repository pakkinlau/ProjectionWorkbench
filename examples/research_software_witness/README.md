# Research/software ProjectLens witness

Generate the deterministic fixture:

```bash
python -m project_semantics witness --output /tmp/project_lens_fixture.json
```

The fixture contains two useful, non-equivalent lenses over one linked project
and one failed commit-author-to-scientific-contributor globalization.
