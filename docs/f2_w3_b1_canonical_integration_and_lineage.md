# F2.W3.B1 Canonical Integration and Lineage Reconciliation

Status: `INTEGRATION_CANDIDATE_READY`

## Bound implementation lines

| Role | Branch / commit | Pull request | Disposition |
|---|---|---|---|
| Canonical integrated candidate | `agent/f2-w2-open-work-kernel-v0` at `2d5af1ea356258bd163e55bbf30cf988da5e2970` before this record | #2 | Retain as the only current integration candidate |
| Bounded ProjectLens predecessor | `agent/f2-w2-k1-m4-project-lens-v0` at `b980eaa17dbe61971b405a38e88b442996e9104c` | #1 | Preserve as predecessor evidence; do not merge as a second canonical package |

Both lines descend from `main@68a8e4100d41f178d9ccb85e3083f172ed512171`.

## Canonical ownership

The integrated K1 line owns the current `project_semantics` package and its public first-witness API:

- `ProjectRef` and external source-link semantics;
- `TaskContext` and derived task-relative lens records;
- local episodes and bounded contribution-state records;
- module and connector contracts;
- noncompensatory composition receipts;
- selective projections and local canonical-history custody;
- deterministic witness replay and packaging.

The M4 line remains the source-bound predecessor witness for:

- explicit ProjectLens plurality;
- local chart and mapping experiments;
- information loss distinct from obstruction;
- lawful unsafe-globalization / non-identifiability / grammar-insufficiency outcomes.

Its `representation.py`, fixtures, package exports, and duplicate public names are **not** a second canonical owner. They remain available at the immutable predecessor commit and may supply future tests or design evidence.

## Semantic mapping

| M4 bounded object | Integrated K1 ownership / projection |
|---|---|
| `ProjectRef` | canonical local project reference |
| `ProjectSnapshot` | content-addressed local source/episode snapshot used by lens derivation |
| `TaskContext` | canonical task-bound input record |
| `ProjectLens` | derived lens payload; never canonical project history |
| `DomainChart` | extension semantics carried through task/lens semantic fields and fixture contracts |
| `InformationLossReport` | lens/composition information-loss payload |
| `LensMapping` | connector semantic mapping / relation record |
| `GlobalizationResult` | fail-closed composition or representation disposition |

This mapping is claim-bounded. It does not assert one-to-one API compatibility or universal semantic equivalence.

## Migration effect

- No released package or admitted external data format exists, so no production data migration is authorized or required.
- Do not merge PR #1 into PR #2 as code: both introduce overlapping package metadata and public type ownership from the same base.
- Existing M4 fixtures should be ported selectively only when a later branch identifies a missing regression witness in the integrated line.
- New local work should target the integrated K1 package and regenerate derived views from local canonical state.

## Rollback

- Immutable rollback source: `b980eaa17dbe61971b405a38e88b442996e9104c`.
- If the integrated candidate later fails an accepted M4 semantic witness, reopen the lineage decision, port the smallest missing test/contract, and avoid restoring duplicate root types.
- Closing or superseding PR #1 does not delete its commit or historical branch evidence.

## Exact reentry

1. review the integrated PR #2 at its current head;
2. verify that later Wave-3 semantic/adversarial witnesses do not expose a regression against accepted M4 evidence;
3. merge PR #2 only through an explicit owner decision;
4. retain PR #1 as closed/superseded predecessor evidence;
5. route any discovered semantic gap through a new bounded repair branch rather than reviving two canonical package lines.

## Claim boundary

This record establishes repository-lineage and canonical-type ownership readiness only. It does not establish product value, market demand, universal semantics, scientific validity, package release, owner acceptance, or Wave-3 completion.
